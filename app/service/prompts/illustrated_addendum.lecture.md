## 本场景的取舍

课堂录屏里，画面通常补上的是这些：完整路径、准确版本号、报错的核心那一行、菜单项与勾选项的名称、现场敲下的命令与它的输出。这些内容在转写里几乎全是错的或空的，按画面上的字写。

图注参照：

```
![终端里的 pip 安装输出](assets/fig-01.jpg)
图：pip 把包装进了 `d:\biye\python37\lib\site-packages`，装到的 langchain 只有 0.0.27，说明这台机器的 pip 指向的是 Python 3.7 旧环境 `00:02:52`
```

配图价值最高的位置：「你问过的问题」「需要回头确认的地方」这两段里，凡是答案就在屏幕上的地方。
