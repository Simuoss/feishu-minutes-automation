fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0
httpx>=0.27.0
lark-oapi>=1.4.0
PyJWT>=2.9.0
# 配图纪要：筛选视频截图（抽帧本身依赖外部 ffmpeg，不走 pip）
Pillow>=10.0.0

# 导出：纪要 pdf/docx，转写 md/txt
# python-docx 与 pypdf 兼作本地导入的文档解析
python-docx>=1.1.0
pypdf>=4.0.0
markdown>=3.5.0
xhtml2pdf>=0.2.11

# Cloudflare R2（S3 兼容）
aioboto3>=13.0.0

# 开发依赖
pytest>=8.0.0
